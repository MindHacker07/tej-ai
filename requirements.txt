# TejStrike AI Requirements
# Core dependencies (all cross-platform)
# No external dependencies required for base functionality!
# TejStrike AI is built with Python standard library only.

# Optional: Enhanced readline for Windows
# pip install pyreadline3     # Better tab completion on Windows

# Optional: Rich terminal UI (if you want enhanced formatting)
# pip install rich            # Beautiful terminal formatting

# ─── LLM Providers (install the one you want to use) ───
# pip install anthropic       # Anthropic Claude (Opus, Sonnet, Haiku)
# pip install openai          # OpenAI GPT (GPT-4o, GPT-4, o1)
# pip install groq            # Groq fast inference (Mixtral, LLaMA)
# pip install ollama          # Ollama local models (optional client)

# Install all LLM providers at once:
# pip install anthropic openai groq

# ─── MCP Server Support (built-in, no extra deps) ───
# MCP uses JSON-RPC over stdio/SSE - no additional packages needed
